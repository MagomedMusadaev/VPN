version: '3.3'

services:
  bot-vpn:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: bot-vpn
    env_file:
      - ../.env # Подключаем .env файл из корня проекта
    volumes:
      - ../.env:/../.env # Монтируем .env файл внутрь контейнера, если нужно
    ports:
      - "8080:8080"
    command: ["./main"]
