version: '3.3'

services:
  bot-vpn:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: bot-vpn
    ports:
      - "8085:8085"
    env_file:
      - docker/.env
    command: ["./main"]
